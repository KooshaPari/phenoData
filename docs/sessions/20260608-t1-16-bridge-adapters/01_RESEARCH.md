# Research

- Verified current workspace only contained `pheno-query`, `surreal-bridge`, `pg-bridge`, and `smoke-tests`.
- Verified no existing `pheno-data-core` crate or `Dataset`/`Writer` traits were present, so a minimal port crate is required to satisfy the requested adapter implementations.
