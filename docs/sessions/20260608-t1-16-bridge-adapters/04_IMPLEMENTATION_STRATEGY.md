# Implementation Strategy

- Keep existing bridge APIs intact.
- Add generic adapter types for the new ports without forcing a larger refactor.
- Use JSON payloads as the bridge-neutral record format.
