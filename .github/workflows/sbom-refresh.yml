name: Monthly SBOM Refresh
permissions:
  contents: read
  pull-requests: read
on:
  schedule:
    - cron: '0 0 1 * *'  # 1st of each month
  workflow_dispatch:

jobs:
  call-sbom-refresh:
    uses: KooshaPari/phenotype-tooling/.github/workflows/sbom-monthly.yml@9dee6790f8c303d8c4d600533ccc895d46c7b9fe
    with:
      repo_path: '.'
      build_system: 'rust'
