name: Monthly SBOM Refresh
permissions:
  contents: read
  pull-requests: read
on:
  schedule:
    - cron: '0 0 1 * *'  # 1st of each month
  workflow_dispatch:

jobs:
  call-sbom-refresh:
    uses: KooshaPari/phenotype-tooling/.github/workflows/sbom-monthly.yml@c43cc4af2cbcc2bb2df37f3e4ab78cc5d8c1b3ad
    with:
      repo_path: '.'
      build_system: 'rust'
