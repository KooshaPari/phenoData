name: Monthly SBOM Refresh
permissions:
  contents: read
  pull-requests: read
on:
  schedule:
    - cron: '0 0 1 * *'  # 1st of each month
  workflow_dispatch:

jobs:
  call-sbom-refresh:
    uses: KooshaPari/phenotype-tooling/.github/workflows/sbom-monthly.yml@587805e38800be98e49f363738ffe314beb52782
    with:
      repo_path: '.'
      build_system: 'rust'
